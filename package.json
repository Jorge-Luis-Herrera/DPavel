{
  "name": "dpavel-unified",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && mkdir -p client && cp -rv ../frontend/out/* ./client/",
    "start": "cd backend && npm run start:prod"
  }
}
